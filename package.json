{
  "name": "automated-co-whatsapp-ai-poc",
  "version": "0.3.0",
  "private": true,
  "description": "Policy-enforced WhatsApp MCP server with durable structured conversation history, signed Meta webhook, and legacy Twilio advisor POC.",
  "type": "module",
  "scripts": {
    "dev": "node ./node_modules/tsx/dist/cli.mjs watch src/server.ts",
    "tunnel": "cloudflared tunnel --url http://localhost:3000",
    "start": "node dist/server.js",
    "clean": "node -e \"fs.rmSync('dist',{recursive:true,force:true})\"",
    "build": "node -e \"fs.rmSync('dist',{recursive:true,force:true})\" && node ./node_modules/typescript/bin/tsc -p tsconfig.json",
    "typecheck": "node ./node_modules/typescript/bin/tsc --noEmit",
    "check:env": "node ./node_modules/tsx/dist/cli.mjs src/local/checkEnv.ts",
    "test": "node ./node_modules/vitest/vitest.mjs run",
    "test:watch": "node ./node_modules/vitest/vitest.mjs",
    "verify": "npm run typecheck && npm test && npm run build",
    "agent:mock": "node ./node_modules/tsx/dist/cli.mjs src/local/mockAgentCli.ts",
    "agent:codex": "node ./node_modules/tsx/dist/cli.mjs src/local/codexAgentCli.ts",
    "demo": "node ./node_modules/tsx/dist/cli.mjs src/local/demo.ts",
    "test:mcp-policy": "node ./node_modules/vitest/vitest.mjs run src/mcp",
    "test:meta-webhook": "node ./node_modules/vitest/vitest.mjs run src/meta/webhook.test.ts",
    "verify:mcp": "npm run typecheck && npm run test:mcp-policy && npm run test:meta-webhook && npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "dotenv": "^16.4.7",
    "express": "^5.2.1",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.5",
    "cloudflared": "^0.7.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": ">=20"
  }
}
